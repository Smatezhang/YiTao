<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.zhuoxin.zhang.yitao">

    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 相机权限 -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- SD卡读写权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        xmlns:tools="http://schemas.android.com/tools"
        android:name=".view.activity.ShopApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:replace="android:name"
        >
        <activity android:name=".view.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.MainActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".view.me.LoginActivity"/>
        <activity android:name=".view.me.RegisterActivity"/>
        <activity android:name=".view.me.UserInfoActivity"/>
        <activity android:name=".view.shop.GoodsDetailsActivity"/>
        <activity android:name=".view.me.PersonInfoActivity">
        </activity>
        <activity android:name=".view.me.NickNameActivity">
        </activity>
        <activity android:name=".view.shop.GoodsUpdateActivity"/>
        <activity android:name=".view.shop.GoodsUploadImageShowActivity"/>
        <activity android:name=".view.me.persongoods.PersonGoodsActivity"/>
    </application>

</manifest>